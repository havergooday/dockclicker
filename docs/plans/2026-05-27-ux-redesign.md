# UI/UX 전면 재설계 계획

작성일: 2026-05-27  
상태: 설계 확정 → 구현 예정

---

## 배경 및 목표

### 현재 구조의 문제
- 1920×300px 형태임에도 패널이 전체를 *교체*하는 구조 → 이전 맥락 완전 소실
- 패널 6개(브릿지·격납고·파견관제·클리커·공작실·상점): 뎁스 최대 3단계
- 기능이 흩뿌려져 있어 "파견 내리기 → 수령"에 패널 이동 필요

### 설계 원칙
1. **브릿지 공간 탐색 방식 유지** — 오브젝트 클릭 → 패널 플립, 위치 이동 기능 포함
2. **패널 수 축소로 뎁스 해소** — 4개 서브패널 → 2개로 통합, 최대 2레벨
3. **패널 내부는 슬라이드인으로 뷰 전환** — 슬라이드 방향은 모든 패널에서 통일
4. **글로벌 컨텍스트 항상 노출** — 크레딧·파견 현황은 어느 패널에서도 보임

---

## 패널 구조 확정

```
현재 (4 서브패널)            →    변경 (2 서브패널)
────────────────────────────      ──────────────────────────
격납고    ┐                         관제 콘솔 클릭
파견 관제 ┘  →  [운영 패널]         → [운영 패널]
공작실    ┐
PC 터미널 ┘  →  [제작 패널]         PC 터미널 클릭
                                    → [제작 패널]
```

| 이동 방식 | 목적지 | 흡수된 기능 |
|---|---|---|
| 브릿지 관제 콘솔 클릭 | 운영 패널 | 파견 관제 + 격납고 + 직접 파견 클리커 |
| 브릿지 PC 터미널 클릭 | 제작 패널 | 공작실 + 상점(파츠·파일럿·함선강화) |

- 브릿지: 파일럿 로밍 공간 유지, 오브젝트 위치 이동 기능 유지
- 뎁스: 브릿지 ↔ 패널 2레벨 고정 (패널 내부는 슬라이드인으로 처리)
- 클리커: 독립 패널 제거 → 운영 패널 내부 상태로 흡수

---

## 슬라이드인 공통 규칙

- **속도**: 빠른 슬라이드 (0.18~0.22s, ease-out cubic)
- **방향**: 모든 패널에서 동일 방향 통일 (방향은 구현 시 확정)
- **닫기**: 역방향 슬라이드로 복귀
- anchor Tween 방식 유지 (현재 파견 관제·격납고 패턴 재사용)

---

## 브릿지 (변경 최소)

- 파일럿 로밍 애니메이션 유지
- 오브젝트 2개: 관제 콘솔, PC 터미널 (위치 드래그 가능)
- 하단 파견 현황 스트립: 파견중 N · 귀환완료 N (숫자만, 타이머 없음)
- 우측 유틸 바 (설정·사운드·최소화) 유지

---

## [운영] 패널 상세

파견 전 과정(출격→진행→수령)이 이 패널 하나에서 해결된다.

### 상태 1 — 기본: 행성 선택

행성을 고르는 단계이므로 행성 영역이 넓게, 베이는 우측에 미리보기 수준으로

```
┌───────────────────────────────────────┬─────────────┐
│  행성 선택 영역                         │  베이 그리드  │
│                                         │  (미리보기)  │
│  [섹터A ●] [섹터B] [섹터C]              │  [1][2][3]  │
│                                         │  [4][5]...  │
│  섹터A — 적 HP 2 · CR/킬 10 · 웨이브50 │             │
│                                         │             │
│  [직접 출격 ▶▶]  [파견 ▶]             │             │
└───────────────────────────────────────┴─────────────┘
         ~75% 폭                             ~25% 폭
```

### 상태 2 — 행성 선택 후: 베이 그리드 확장 (anchor Tween)

행성 선택 완료 → 행성 영역 축소, 베이 그리드가 메인으로 확장

```
┌──────────────┬──────────────────────────────────────┐
│  행성 compact │  베이 그리드 (16슬롯 가로 스크롤)      │
│              │                                        │
│  [A●][B][C]  │  [BAY1●][BAY2][BAY3][BAY4]...        │
│              │  (귀환완료 슬롯 펄스 표시)              │
│  [직접출격]  │                                        │
│  [파견▶]    │                                        │
└──────────────┴──────────────────────────────────────┘
    ~25% 폭                  ~75% 폭
```

취소 버튼(또는 행성 영역 클릭) → 상태 1로 역방향 tween 복귀

### 상태 3 — 파견 모드 (파견▶ 클릭)

기존 파견 관제의 anchor Tween 2-state 그대로 재사용  
planet_zone compact + slot_zone 슬라이드인

### 상태 4 — 베이 상세 (베이 카드 클릭)

기존 격납고의 좌측 슬라이드인 그대로 재사용  
상세 패널 30% + 베이 그리드 70%

수령 플로우: 베이 그리드에서 귀환완료 슬롯 클릭 → 상세 패널 → [수령▶]  
(브릿지→격납고 이동 불필요)

### 상태 5 — 직접 출격 모드 (직접출격▶▶ 클릭)

클리커 전투 영역이 패널 전체를 덮으며 슬라이드인

```
┌───────────────────────────────────────────────────────┐
│  섹터A  처치: 12/50  보류: 120 CR          [복귀]      │
├───────────────────────────────────────────────────────┤
│                                                         │
│    👾        👾      👾          👾       👾            │
│         👾                  👾                          │
│                                                         │
└───────────────────────────────────────────────────────┘
```

복귀 → 슬라이드아웃, 상태 2(베이 그리드 확장 상태)로 복귀  
수익 수령은 복귀와 동시에 자동 처리

---

## [제작] 패널 상세

파츠 구매 → 인벤토리 확인 → 조립이 이 패널 하나에서 해결된다.

### 레이아웃 — 좌측 카테고리 메뉴 + 우측 콘텐츠

```
┌──────────────┬────────────────────────────────────────┐
│  파츠 구매   │                                          │
│  ──────────  │   선택한 카테고리 콘텐츠                  │
│  조립        │   (현재 PC 터미널 우측 콘텐츠 구조 유지)  │
│  파일럿      │                                          │
│  함선 강화   │                                          │
│  ──────────  │                                          │
│  (시설)     │                                          │
│  (꾸밈)     │                                          │
└──────────────┴────────────────────────────────────────┘
   ~180px               나머지 전체
```

| 카테고리 | 흡수 출처 | 내용 |
|---|---|---|
| 파츠 구매 | PC 터미널 파츠 탭 | 몸체/무기/다리 구매 리스트 |
| 조립 | 공작실 파츠조립 탭 | 5열 레이아웃 + SYS SPEC |
| (조립 내) 인벤토리 | 공작실 인벤토리 탭 | 조립 카테고리 내 서브탭 |
| 파일럿 | PC 터미널 파일럿 탭 | 고용·커스텀 생성 |
| 함선 강화 | PC 터미널 함선강화 탭 | 클릭 데미지·자동공격 |

"파츠 구매 후 바로 조립" 플로우: 카테고리 메뉴에서 [조립] 클릭하면 인벤토리 즉시 반영

---

## 씬 구조 변경 계획

### 제거
- `scenes/ui/panel_hangar.tscn` + `scripts/ui/panel_hangar.gd`
- `scenes/ui/panel_dispatch.tscn` + `scripts/ui/panel_dispatch.gd`
- `scenes/ui/panel_workshop.tscn` + `scripts/ui/panel_workshop.gd`
- `scenes/ui/panel_shop.tscn` + `scripts/ui/panel_shop.gd`
- `scripts/ui/panel_clicker.gd` 독립 패널 역할 (로직은 운영 패널로 이식)

### 추가
- `scenes/ui/panel_operation.tscn` + `scripts/ui/panel_operation.gd`
- `scenes/ui/panel_craft.tscn` + `scripts/ui/panel_craft.gd`

### 유지
- `scenes/ui/bridge_view.tscn` (오브젝트 2개로 정리)
- `PanelManager` (bridge ↔ operation ↔ craft 3개 패널만 관리)
- `credit_hud.tscn` (글로벌 HUD)

---

## 구현 순서

### Phase 1 — 운영 패널
가장 핵심이고 플레이 임팩트가 큼

1. `panel_operation.tscn` 씬 + 기본 레이아웃 (상태1: 행성 넓게)
2. 상태1↔2 anchor Tween (행성 축소/베이 확장)
3. 베이 그리드 + 상세 슬라이드인 (hangar 로직 이식)
4. 파견 모드 슬라이드인 (dispatch 로직 이식)
5. 직접 출격 모드 (clicker 로직 이식)

### Phase 2 — 제작 패널

1. `panel_craft.tscn` 씬 + 좌측 메뉴/우측 콘텐츠 구조
2. 파츠 구매 (shop 이식)
3. 조립 + 인벤토리 (workshop 이식)
4. 파일럿 / 함선 강화 (shop 이식)

### Phase 3 — 브릿지 정리

1. 오브젝트를 관제 콘솔 / PC 터미널 2개로 정리
2. 파견 현황 스트립 (파견중 N · 귀환완료 N)
3. 구버전 씬/스크립트 제거

### Phase 4 — 문서 갱신 및 정리

---

## 미결 사항

- [ ] 슬라이드인 통일 방향: 우측에서? 하단에서? (구현 시 테스트 후 확정)
- [ ] 운영 패널 상태1 기본값: 첫 진입 시 행성 미선택 상태 vs 마지막 선택 행성 유지
- [ ] 브릿지 파견 현황 스트립 위치: 하단 고정 vs 우측 유틸 바 옆

---

## 피드백 (2026-05-27)

### 운영 패널 — 베이 카드 레이아웃 스케일 문제
베이 수가 16개, 32개로 늘어날 경우 가로 카드 1행 레이아웃으로는 대응 불가.
슬롯 수가 많아져도 한눈에 파악할 수 있는 레이아웃 방식 재검토 필요.

### 운영 패널 — 진행 순서 수정
현재 와이어프레임은 베이 현황을 기본 진입 상태로 잡았으나,
실제 플레이 순서는 **행성 선택 → 베이 선택** 이 맞음.
나중에는 여러 베이를 동시 선택해 한꺼번에 파견하는 흐름도 고려.
→ 운영 패널 기본 진입 상태를 행성 선택으로 변경 검토.

### 직접 파견 — 플레이어 전용 베이
임무 결과 화면을 별도 씬으로 만드는 것보다,
직접 파견도 "플레이어 머신의 임무"로 취급해 **플레이어 전용 베이(BAY 0)** 를 하나 두는 방향 검토.
귀환 후 베이에 플레이어 머신이 대기 중인 모습으로 표현 → 별도 결과 화면 불필요.
자동 파견 베이와 동일한 수령 UX 흐름으로 통합 가능.

### 제작 패널 — 파츠 구매 UI
현재 리스트 행 나열 방식은 밀도가 높고 게임답지 않음.
다른 표현 방식 검토 필요 (예: 카드형, 도감형, 장비창형 등).

### 제작 패널 — 조립 UI
현재 5열 스프레드시트 형태는 게임 느낌 부족.
더 게임스러운 조립 인터페이스 방향 검토 필요
(예: 머신 실루엣에 파츠를 붙이는 방식, 슬롯 드래그 등).
