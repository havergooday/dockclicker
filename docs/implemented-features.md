# 구현된 기능

## 개요

이 문서는 현재 저장소에 실제로 반영된 기능, 구조, 문서를 정리한다.  
여기서 말하는 `구현된 기능`은 플레이 가능한 게임 기능뿐 아니라, 프로젝트 운영에 필요한 문서/구조/검증도 포함한다.

## 문서화 완료

- 파견 중심 게임 기획 문서 작성 완료
- 가로형/세로형 와이어프레임 및 비주얼 방향 문서 작성 완료 (상태 표시줄 위치 옵션 기획 포함)
- Godot 프로젝트 구조 문서 작성 완료
- Godot 부트스트랩 구현 계획 문서 작성 완료
- 문서 허브 및 상태 문서 작성 완료

## 프로젝트 구조 구현 완료

- `Godot 4.6` 프로젝트 파일 생성 완료
- 기본 아이콘 파일 생성 완료
- `assets/`, `data/`, `docs/`, `scenes/`, `scripts/`, `tests/` 폴더 구조 생성 완료
- Python 캐시 및 Godot 생성물 무시용 `.gitignore` 설정 완료

## 코드/씬 스캐폴드 구현 완료

- 메인 씬 `scenes/main/main.tscn` 생성 완료
- 앱 루트 스크립트 `scripts/core/app_root.gd` 생성 완료
- 전역 상태 오토로드 `scripts/autoload/game_state.gd` 생성 완료

## 파노라마 함선 캔버스 구현 완료

- `ShipCanvas` 메인 루트 구현 완료
  - 좌측 격납고 / 중앙 브릿지 / 우측 관제실 3구역을 한 화면에 배치 (전체 높이 채움)
  - 우클릭 드래그로 캔버스 이동
  - 탭 버튼 (라디오 그룹, 토글)으로 구역 이동 + 활성 탭 강조
  - 기본 진입 시 브릿지로 스크롤 이동
- **격납고 구역 (`HangarZone`) 구현 완료** (`scripts/ui/hangar_zone.gd`)
  - 헤더 없이 전체 구역 사용 (nav bar 하단~화면 하단)
  - 베이 그리드: 가로 스크롤, 좌클릭 드래그 탐색
  - **4개 격납고 × 4 베이 구조**: 격납고 0은 기본 해금, 격납고 1~3은 별도 해금 비용 필요
  - 격납고 간 24px 구분선 (GROUP_SEP) 으로 시각적 구분
  - 초기 스크롤 우측 고정, 리빌드 시 스크롤 위치 보존
  - 베이 카드 (108×108): BAY번호, 상태(LOCKED/EMPTY/OFFLINE/ON MISSION/RETURNING/RETURNED), 스프라이트 영역, 귀환 크레딧/파일럿명 표시
  - 귀환 완료 베이 → 맥동 글로우 (Tween 루프)
  - 베이 클릭 → 전역 오버레이 상세 팝업 방식으로 상세 표시 (슬라이드인 패널 방식 대체)
    - 팝업 열린 동안 드래그 스크롤 비활성
    - 리빌드 후 팝업 복원 (리빌드 전 슬롯 기억)
  - 팝업 상세 — 상태별 내용:
    - LOCKED (격납고): 격납고 해금 비용 + 인라인 확인 버튼
    - LOCKED (베이): 베이 해금 비용 + 해금 버튼
    - EMPTY: 공작실에서 조립 버튼 (기존 패널로 임시 연결)
    - OFFLINE: 파일럿 초상화 + 머신 실루엣 + 전체 스펙 + 파츠 옵션 요약
    - ON MISSION/RETURNING: 파일럿·행성·예상 수익을 포함한 전체 스펙 + 파츠 옵션 요약
    - RETURNED: 귀환 크레딧 + 수령 버튼
  - **전역 베이 상세 팝업** (`scenes/ui/hangar_bay_popup.tscn`)
    - 화면 폭의 60%를 차지하는 전역 오버레이 팝업
    - 상단 중앙 닫기 버튼, 위에서 아래로 내려오는 슬라이드 애니메이션
    - 좌측 파일럿 / 중앙 장비 슬롯 / 우측 전체 스펙 + 파츠 옵션 / 우측 버튼 컬럼 구성
    - 중앙 장비칸은 RPG 장비창 느낌의 3슬롯 배치로 축소
    - 각 슬롯은 실제 파츠 스프라이트를 꽂을 수 있는 `TextureRect` 자리와 짧은 캡션으로 분리
    - 우측 버튼 컬럼은 상태별 컨텍스트 액션(조립 완료/수리 예정/관제실 이동/수령/해금)을 표시
    - 빈 슬롯/도킹 슬롯 클릭 시 우측 슬라이드 드로어로 파일럿 목록 또는 파츠 인벤토리를 선택하는 구조를 추가
    - **드로어 우클릭 드래그 스크롤** — 마우스휠 비활성, 우클릭 드래그로 파일럿/파츠 목록 스크롤
    - **드로어 너비 동적 계산** — 뷰포트 너비 50% 기준, 파츠 장착 패널 근처까지 커버
    - **장착 해제 버튼** — 이미 파일럿/파츠가 배정된 경우 목록 맨 위에 붉은 계열 "장착 해제" 버튼 표시
    - **조립중 상태 지속** — 팝업을 닫아도 `AutoSlot.pending_machine` / `pending_pilot_id`에 드래프트 보존, 재오픈 시 복원
    - **ASSEMBLING 베이 카드 표시** — 진행 중인 조립이 있는 empty 슬롯은 주황 ASSEMBLING 배지로 구분
    - **베이 전환 오염 버그 수정** — `_init_draft_state()` 가 이전 베이 값을 먼저 초기화하도록 수정
    - **동일 티어 파츠 교체 허용** — `replace_machine_part()` 동일 tier early return 제거, 인스턴스 스왑 동작
    - `remove_machine_part()` 추가 — offline 슬롯의 특정 파츠를 인벤토리로 반환하고 슬롯 갱신
  - 격납고 Bay의 도킹 상태 UI는 재조립 버튼 없이 슬롯 클릭 교체로 처리하고, 수리는 예정 항목으로만 남김
  - 파견 중 Bay는 임무 정보, 도착 행성, 남은 시간만 노출하는 읽기 전용 요약으로 유지
  - 귀환 완료 Bay는 수령 버튼과 보상 요약만 제공
  - "관제실에서 파견" → ShipCanvas signal으로 관제실 스크롤 + 항성지도 팝업 오픈
  - 구역 경계에 16px 격벽 (격납고↔브릿지, 브릿지↔관제실)
- 관제실 `항성지도` 팝업 구현 완료 (오버레이 + 슬라이드 패널 구조)
  - 아이콘형 1행 가로 스크롤 행성 그리드 — 색상 코딩, 잠금/해금 상태 표시
  - 우클릭 드래그로 행성 리스트 탐색, 마지막 스크롤 위치 복원
  - 마지막 선택 행성 기본 복원 (`GameState.selected_planet` 연동)
  - 행성 클릭 → 슬라이드 패널 (X | 행성상세 | 행성슬롯) 우측에서 슬라이드인
  - **행성 슬롯**: 58×58 정사각형 카드, 4행 그리드, 우클릭 드래그 가로 스크롤
    - 빈 슬롯(+) 클릭 → 베이 팝업 오픈
    - 파견 중/복귀 중 슬롯 → 비활성 표시 (▶/◀ 아이콘)
    - 복귀 완료 슬롯 → "복귀" 텍스트 표시, 클릭 불가 (수령은 격납고에서)
  - **베이 팝업**: 슬롯 영역 위로 슬라이드인 (X | 베이 그리드), 우클릭 드래그 가로 스크롤
    - 베이 카드 (96×100): BAY 번호 + 파일럿 이름 + 출격/상태 버튼
    - 대기 중 → [출격] 버튼 (즉시 파견) — 파일럿 없거나 티어 부족 시 비활성화
    - 파견 중/복귀 중 → 상태 텍스트, 복귀 완료 → "복귀 완료" 텍스트
    - 파견 후 베이 팝업 닫힘 → 해당 슬롯 비활성으로 갱신
  - 직접 파견 버튼 → 클리커 화면 진입 (기존 PanelManager 경유)
  - ESC/뒤로가기: 단계별 닫힘 (베이 팝업 → 슬라이드 패널 → 팝업 닫힘)

## 패널 플립 내비게이션 시스템 구현 완료

- `PanelManager` autoload — 패널 전환 총괄, 세로 Tween 플립 (0.12s × 2)
- **히스토리 스택 내비게이션** — `_history: Array[String]` + `go_back()`: 이전 패널로 순차 복귀
  - 격납고 → 공작실 진입 후 뒤로가기 → 격납고 복귀 (브릿지가 아닌 직전 패널)
  - ESC 키도 `go_back()` 적용 — 히스토리 순서대로 역행
- **동적 뒤로가기 버튼 라벨** — 각 패널의 "← 브릿지" 대신 `get_back_label()`로 직전 패널명 표시
- 브릿지 뷰 (`scenes/ui/bridge_view.tscn`) — 인터랙션 오브젝트 4개(격납고 문, 공작실 입구, PC 터미널, 관제 콘솔)
- 서브 패널 4개 — 각 패널별 뒤로가기 버튼
  - 격납고 (`scenes/ui/panel_hangar.tscn`)
  - 공작실 (`scenes/ui/panel_workshop.tscn`)
  - PC 터미널 (`scenes/ui/panel_shop.tscn`)
  - 파견 관제 (`scenes/ui/panel_dispatch.tscn`)
- `scenes/main/main.tscn` — 6개 패널 인스턴스 구조 (브릿지 + 서브 4 + 클리커)

## 테스트 및 검증 구현 완료

- 구조 검증 테스트 `tests/test_project_structure.py` 작성 완료
- 필수 파일 존재 여부 검증 가능
- `Godot 4.6` 표기 검증 가능

## 도커형 창 구성 완료

- 테두리 없는 창 (`borderless`) — 제목 표시줄 제거
- 항상 위 (`always_on_top`) — 다른 창 위에 유지
- 크기 고정 (`resizable=false`)
- 시작 시 `usable_rect` 기준 하단 정렬 자동 배치 (작업 표시줄 위에 딱 붙음)
- 빈 영역 드래그로 창 위치 이동 (`_unhandled_input` 기반)

## 배경 이미지 및 반투명 UI 시스템 구현 완료

- 4개 패널에 픽셀 아트 배경 이미지 적용 (`TextureRect`, `stretch_mode=SCALE`)
  - `bridge_bg.png`, `hangar_bg.png`, `dispatch_bg.png`, `workshop_bg.png`
- PC 터미널은 배경 이미지 없이 어두운 단색 배경 + 반투명 UI 레이어 사용
- 각 패널에 반투명 암막 오버레이 (`ColorRect`, `Color(0,0,0,0.45)`) 추가
- 모든 StyleBoxFlat `bg_color`에 alpha 0.78~0.88 적용 — 배경이 UI 뒤로 비침

## 브릿지 우측 유틸 사이드 패널 구현 완료

- 우측 끝에 세로 버튼 스트립 항시 표시: `⚙` (설정) / `♪` (사운드) / `─` (최소화)
- 버튼 클릭 시 200px 사이드 패널이 버튼 좌측에 나타남; 재클릭 시 닫힘
- **⚙ 설정 탭**
  - 항상 위에 ON/OFF (`WINDOW_FLAG_ALWAYS_ON_TOP`)
  - 드래그 이동 ON/OFF (mouse_filter 토글)
  - 창 불투명도 슬라이더 20~100% (AppRoot `modulate.a`)
  - **UI 위치 초기화 버튼** — 드래그로 이동된 인터랙션 버튼 위치를 기본값으로 복원 (`ui_positions_reset` 시그널)
- **♪ 사운드 탭**
  - 전체 음소거 ON/OFF (`AudioServer.set_bus_mute`)
  - 마스터 볼륨 슬라이더 (AudioServer Master 버스)
  - BGM / SFX 버스 추가 시 자동 슬라이더 표시
- **─ 최소화**: `Window.MODE_MINIMIZED`

## 브릿지 — 로밍 파일럿 구현 완료

- 고용된 파일럿이 브릿지 화면을 자유롭게 돌아다니는 애니메이션
- **구현 방식**: Tween 체인 — 이동 → 깊이 업데이트 → 1.5~4초 대기 → 반복 (`_process()` 없음)
- **원근감**: Y 좌표 기반 scale(0.75~1.0) + z_index 변화로 공간감 연출
- **컬러 사각형 플레이스홀더** (56×96px) — 파일럿별 커스텀 색상, 픽셀 아트 스프라이트 교체 예정
- **클릭 말풍선** — 파일럿 클릭 시 이름·상태 말풍선 3.5초 표시 후 자동 소멸
- **파견 연동**: 파견 중 페이드 아웃 + 이동 일시 정지, 귀환 시 복귀

## 직접 파견 클리커 시스템 구현 완료

**플레이 루프:** 브릿지 → 파견 관제 → 직접 출격 → 클리커 → 복귀 → 격납고 수령 → 재화 HUD 증가

- `GameState` — `pending_credits`, `player_status` (idle/on_mission/returned), 시그널
- 클리커 화면 (`panel_clicker.gd/tscn`)
  - 외계생물 버튼 랜덤 위치 배치 (BattleArea 내 자유 좌표)
  - 행성별 동시 최대 출현 수 / 웨이브 총 마리수 적용
  - 클릭 → HP 감소 → 처치 → 보류 크레딧 적립
  - **자동 공격**: 자동 공격 해금 시 1.5초마다 임의 적에게 click_damage/2 데미지 (파란 플래시 연출)
  - 웨이브 완료 → 화면 중앙 **"미션완료"** 플로팅 텍스트 0.8초 표시 후 자동 복귀
  - 복귀 버튼 / ESC → **"함선복귀"** 플로팅 텍스트 0.5초 표시 후 복귀 (중복 호출 방지)
  - **복귀 버튼 헤더 배치** — Header HBoxContainer 내 우측에 배치 (하단 제거)
  - 헤더에 행성명·처치수·보류 크레딧 실시간 표시
  - 복귀 시 `collect_player_credits()` 자동 호출 → player_status idle 복귀 + 크레딧 수령
- 격납고 패널 — 플레이어 슬롯 상태(대기중/임무중/귀환완료), 수령 버튼

## 글로벌 크레딧 HUD 구현 완료

- 화면 하단 **플로팅 pill 오버레이** — 공간 점유 없이 모든 패널 위에 띄움 (`credit_hud.tscn`)
- 현재 보유 CR 표시, 변경 시 숫자 트윈 애니메이션
- 수령 시 +금액 아이콘 날아오는 연출
- 모든 패널 `offset_bottom = 0.0` — 전체 화면 높이 활용 (구 고정 26px 바 제거)

## 행성 시스템 구현 완료

섹터 A~T 총 20개 행성, 각 행성에 `max_slots` (동시 파견 제한) 추가

| 행성 | 해금 비용 | max_slots |
|---|---|---|
| 섹터 A~D | 0 ~ 800 CR | 10 (테스트) |
| 섹터 E~H | 3,000 ~ 120,000 CR | 12 (테스트) |
| 섹터 I~L | 400,000 ~ 18,000,000 CR | 14 (테스트) |
| 섹터 M~T | 65,000,000 CR ~ | 16 (테스트) |

- 파견 관제 패널에서 행성 선택 UI
- 미해금 행성 클릭 → 크레딧 차감 + 자동 해금 + 선택
- 크레딧 부족 시 버튼 비활성화

## PC 터미널 — 상점 UI 구현 완료

- **좌측 카테고리 메뉴 + 우측 콘텐츠 영역** 구조로 전면 개편
  - 좌측 메뉴: `파츠 / 함선 강화 / 파일럿 / 시설(비활성) / 꾸밈(비활성)`
  - 크레딧 수치는 글로벌 HUD가 담당하므로 패널 내부 중복 표시 제거
- **파츠 카테고리** — 몸체 / 무기 / 다리 3섹션을 전폭 리스트 행으로 표시
  - 행 구성: `T# / 이름 / 효과 / 해금 조건 / 가격 / 구매 버튼`
  - 보유 수량은 상점에서 노출하지 않음; 인벤토리 확인은 공작실 담당
  - 본문 전체 세로 스크롤 지원 — 300px 높이에서도 하단 항목 확인 가능
  - 행 높이/폰트 압축으로 한 화면 밀도 최적화
- **함선 강화 카테고리** — 전폭 업그레이드 리스트
  - 클릭 데미지 강화 1종 활성
  - **자동 공격 (800 CR)**: 1.5초마다 임의 적에게 클릭 데미지의 절반을 자동 적용, 구매 후 비활성화 불가
  - 클릭 범위 / 무기 교체는 회색 프리뷰 항목으로 표시
  - 업그레이드 목록 세로 스크롤 지원
- **파일럿 카테고리** — 좌측 목록 + 우측 상세 비주얼 2분할
  - 고용 가능 파일럿과 내 파견단을 한 리스트 패널에 통합
  - 상세 패널에서 초상화, 티어, 상태, 보너스, 고용 버튼 표시
  - 커스텀 생성은 별도 상세 폼이 아니라 전용 작업 화면으로 전환
  - 파일럿 목록 / 커스텀 생성 폼도 독립 스크롤 지원

## 파일럿 시스템 구현 완료

- **파일럿 개별 인스턴스** 모델 — 소모품 카운터 방식 제거, 각 파일럿이 이름·티어·보너스·초상화 색상 보유
- **파일럿 풀 36인** (`data/pilots_data.gd`):
  - T1 12명 (500 CR, 기본 해금): 카이라 벡스 / 제이스 롤 / 미라 옌 / 세브 온 / 토베 악스 / 리아 초 / 한 나 / 젤 코 / 페이 백스 / 모라 벨 / 키안 덱스 / 아비 솔
  - T2 12명 (1,500 CR, sector_c 해금 후 등장): 리오 손 / 다엘 리스 / 유라 낙 / 벡스 콘 / 소마 리 / 카이 렌 / 넥스 파르 / 벨락스 린 / 소라 멕스 / 디온 케르 / 펠 아리스 / 지온 바
  - T3 12명 (3,500 CR, sector_f 해금 후 등장): 도나 마르 / 바엘 오른 / 렉스 홀 / 지라 벨 / 카노 셋 / 펠 다르 / 오라 셉 / 나엘 렉스 / 키라 존 / 벨 소른 / 악사 미르 / 렌 코브
  - 각 파일럿에 `flavor` 플레이버 텍스트 추가
- **모집 공고판 시스템** (`GameState`):
  - 매일 자정 자동 갱신 — 오늘의 파일럿 3명 날짜 시드 기반 선택
  - 행성 진행도에 따라 등장 파일럿 티어 상승 (sector_c → T2, sector_f → T3)
  - 이미 고용된 파일럿은 공고판 풀에서 자동 제외, 고용 즉시 공고판 재생성
  - **유료 갱신** (300 CR): 즉시 새 조합 추출, 갱신 횟수로 시드 변형
  - `board_pilot_ids / board_last_day / board_refresh_count` 저장/복원
- **커스텀 파일럿 생성** (create_custom_pilot): 이름 입력 + 6색 선택 + 300 CR, T1 스탯 고정
- **파견 시스템 연동**: 파견 시 pilot.status = "on_mission", 수령 시 "idle" 복귀
- **보너스 적용**: speed 보너스 → 파견 시 mission_end_time 단축, credits 보너스 → _start_returning 시 credits_earned 증가
- **파견 관제 UI**: 파일럿 선택 UI가 티어 버튼 → 이름 버튼으로 교체 (대기중인 파일럿만 표시)
- **PC 터미널 파일럿 탭** (레거시): 고용 가능/보유 파일럿 목록, 커스텀 생성 폼
- **저장/불러오기**: hired_pilots 배열 JSON 직렬화, pilot status 복원

## 관제실 — 모집 공고판 팝업 구현 완료

- **`shop_popup.gd` / `shop_popup.tscn`** — 관제실 "상점/고용" 버튼에서 오버레이 팝업으로 호출
  - 위에서 아래로 슬라이드인 (0.20s ease-out SINE)
  - 배경 딤 클릭으로 닫기
- **SF 인사 파일 카드 뷰어** — 파일럿 1명씩 전체 팝업 너비로 표시, 좌우 슬라이드 내비게이션
  - 카드 레이아웃: 좌측 전신 아트 존 (240px, 파일럿 색상 기반 플레이스홀더) | 우측 3행 구조
    - 행 1: 초상화 원형 + 이름 / TIER + 도트 / 상태 (모집가능 / ON BOARD)
    - 행 2: 보너스 설명 + 플레이버 텍스트 (EXPAND)
    - 행 3: 비용 CR
    - 카드 우측 세로 고정 `고용` 버튼 컬럼
  - 하단 내비: `◀ 이전` / 페이지 도트 / `다음 ▶`
  - 카드 전환 시 0.14s 슬라이드 애니메이션 (이전/다음 방향 반영)
  - 이미 고용된 파일럿: `고용됨` 비활성 버튼
  - 크레딧 부족 시 우측 고용 버튼 비활성
- **갱신 타이머**: 다음 자정까지 HH:MM:SS 카운트다운 실시간 표시
- **유료 갱신 버튼** (300 CR): 즉시 새 공고 생성, 크레딧 부족 시 비활성
- **커스텀 파일럿 생성** — 팝업 하단 버튼에서 인라인 오버레이 폼 호출 (이름/색상/비용 입력)
- 행성 해금 시 자동으로 공고판 풀 재계산

## 브릿지 파일럿 로밍 시스템 구현 완료

- **`bridge_pilot.gd`** — 브릿지 구역에 고용된 파일럿 캐릭터가 배치됨
  - 파일럿 고용 즉시 브릿지에 등장 (`pilot_hired` 시그널 연결)
  - 랜덤 좌우 로밍 — 목표 X 위치로 40px/s 이동, 도착 후 1.5~4초 대기 후 새 목표 선택
  - **클릭 → 말풍선**: 랜덤 인삿말 2.4초 후 자동 소멸
  - **드래그 이동**: 좌클릭 드래그로 파일럿을 원하는 위치로 이동, 놓은 자리가 새 로밍 기준점
  - 파일럿별 고유 색상 원형 플레이스홀더 (48×48px, 픽셀 아트 스프라이트 교체 예정)

## 파츠 개별 아이템 시스템 구현 완료

- `GameState.owned_parts` (수량 딕셔너리) → `part_inventory: Array` (인스턴스 배열) 리팩터링
  - 각 파츠: `{iid, type, tier}` 딕셔너리 단위
  - `buy_part()` → 인스턴스 append, `consume_part()` → 첫 매칭 인스턴스 remove
  - `get_owned_qty()` 는 count 함수로 유지 (하위 호환)
- `SaveManager` 저장 버전 v2 → v3 bump, v2→v3 자동 마이그레이션 내장
- `DispatchManager.assemble_machine()` → `consume_part()` 호출로 전환

## 공작실 패널 UI 구현 완료

### 인벤토리 탭 (신규)
- `[조립]` / `[인벤토리]` 탭 토글 (24px 탭바)
- **60개 이상 아이템 세로 스크롤**: ScrollContainer + 동적 행 생성 (20열 × n행, 아이템 수 초과 시 자동 확장)
- 채워진 슬롯: 타입 아이콘(몸/무/다) + T# 티어, 티어별 컬러 보더
- 빈 슬롯: 어두운 플레이스홀더 (RPG 인벤토리 느낌)
- 클릭 → 우측 260px 상세 패널에 T# 대형 표시, 이름·타입·효과 표시

### 파츠조립 탭
- **5열 가로 레이아웃** — 격납고(110px 고정) / 몸체 / 무기 / 다리 / SYS SPEC(160px 고정)
- **격납고 열**: 빈 슬롯 버튼 배열, 선택 시 강조
- **파츠 열 (몸체/무기/다리)**: 보유 파츠를 **개별 아이템** 단위 버튼으로 표시, ButtonGroup으로 단일 선택
- **SYS SPEC 열**: 선택 요약 + 미션시간·CR/s·복귀시간·예상수익·조립비용 라이브 계산, STANDBY/CR부족/READY 상태 표시
- 조립하기 버튼 (하단 전체 너비, 조건 충족 시 초록 활성화)
- 격납고에서 빈 슬롯 클릭 → 공작실 이동 + 해당 슬롯 자동 선택 (`workshop_preselect_slot`)

## 격납고 패널 구현 완료

- **베이 그리드 (전체 폭, 기본) + 좌측 슬라이드 인 상세 패널** 레이아웃
  - 기본 상태: 그리드가 전체 너비 차지, 상세 패널 숨김
  - 베이 카드 클릭: 상세 패널(30% 너비)이 좌측에서 슬라이드 인, 그리드가 우측 70%로 수축 (0.28s cubic)
  - 같은 카드 재클릭 또는 ✕ 버튼: 역방향 애니메이션 후 패널 닫힘
  - 격납고 나가면 상태 즉시 초기화
  - 뒤로가기 버튼 가림 버그 수정: `move_child`로 BackButton을 BayLayout 위에 유지

### 베이 그리드

- **16슬롯 가로 스크롤 그리드** (8열 × 2행) — 드래그로 좌우 탐색, 스크롤바 없음
  - 해금 비용: 300 → 25,000,000 CR 순차 증가
  - 슬롯 데이터 없는 자리도 플레이스홀더로 채워 2행 구조 항시 유지
- **ITB 스타일 베이 카드** (108×108px 정사각형) — 상태별 보더 색상
  - 상태: 잠금 / 비어있음 / 대기중(offline) / 파견중 / 귀환중 / 귀환완료
  - 귀환완료 상태: 보더 펄스 글로우 애니메이션
  - 선택된 카드: 밝아진 보더 + 두꺼운 테두리로 강조
- **카드 클릭 → 상세 패널 슬라이드 인**; offline 카드에 배정 파일럿 이름 5자 표시

### 상세 패널 (슬라이드 인)

- **잠금**: 해금 비용 표시 + 해금 버튼 (크레딧 부족 시 비활성)
- **비어있음**: 공작실 이동 버튼
- **대기중(offline)**:
  - 머신 스펙 표시 (몸체T·무기T·다리T)
  - 파일럿 배정 섹션 — 대기 중인 파일럿 토글 버튼으로 배정/해제
  - "파견 관제로 ▶" 이동 버튼
  - "머신 분해" 버튼 (빨간 계열) — offline 상태에서만 가능, 파츠 인벤토리로 반환
- **파견중 / 귀환중**: 머신 스펙, 파일럿, 목적지, 예상 수익 읽기 전용 표시
- **귀환완료**: 획득 CR 표시 + "수령 ▶" 버튼

### 파일럿-베이 사전 배정 시스템

- `AutoSlot.assigned_pilot_id` 필드 — 파견과 별개로 베이에 파일럿을 미리 배정 (영속)
- `GameState.assign_pilot_to_slot(slot_index, pilot_id)` — 타 슬롯에 배정된 파일럿이면 자동 이동
- `GameState.disassemble_machine(slot_index)` — 파츠를 인벤토리로 반환하고 베이를 empty로 초기화
- `SaveManager`가 `assigned_pilot_id`를 JSON에 직렬화·복원
- `GameState.slot_pilot_assigned` 시그널 — 배정 변경 시 격납고 UI 재빌드 트리거

## 파견 관제 패널 UI 구현 완료 (재설계)

### 두 상태 레이아웃 (anchor Tween 애니메이션)

**기본 상태 — 행성 선택 (전체 폭)**
- 행성 카드형 버튼 가로 배치 (142×58px) — 선택 시 하단 3px 보더 강조
- 선택 행성 스펙 표시: 적 HP · CR/킬 · 웨이브 수
- [직접 출격 ▶▶] / [파견 ▶] 버튼
- 미해금 행성 클릭 → 해금 확인 팝업 (비용 표시, 크레딧 부족 시 빨간색 + 부족액 표시, 배경 클릭으로 닫기)

**파견 모드 — 행성 축소 + 슬롯 리스트 (anchor Tween 0.28s, ease-out cubic)**
- planet_zone: `anchor_right` 1.0 → 0.20 (compact 패널로 수축)
- slot_zone: `anchor_left` 1.0 → 0.20 (우측에서 슬라이드인)
- 취소 시 역방향 Tween → 전체 폭 복귀 후 행성 UI 재빌드

### compact 행성 패널 (~20% 폭)
- **가로 스크롤 행성 버튼 스트립** — 행성이 많아져도 스크롤로 대응
- 선택 행성 이름 표시
- [직접 출격 ▶▶] 버튼 (하단)

### 슬롯 리스트 (~80% 폭)
- 헤더: "→ 행성명 파견" + [← 취소] 버튼
- **세로 스크롤 리스트** — 16슬롯 전체 표시 (하드코딩 3슬롯 제거, Dictionary 기반 타이머 refs)
- 각 행: BAY # · 파일럿명 · 머신 T# · 상태/타이머 · 액션 버튼
  - **파견 가능** (offline + 파일럿 배정 + idle + 행성 접근 가능): [파견▶] 활성, 밝은 스타일
  - **파일럿 미배정**: 흐린 스타일, 버튼 없음
  - **on_mission / returning**: 프로그레스바 + MM:SS 실시간 카운트다운
  - **returned**: "+N CR" 표시 + [수령▶] (격납고 이동 불필요)
  - **locked / empty**: 흐린 스타일, 버튼 없음
- 파견 선택 흐름 단순화: 격납고 `assigned_pilot_id` 기반 → 파견 관제 내 파일럿 선택 단계 제거

## 자동 파견 타이머 시스템 구현 완료

- **임무 단계** (`on_mission`): 몸체 파츠 가치(초) 동안 진행
- **귀환 단계** (`returning`): 기본 30초 − 다리 파츠 감소값 (최소 5초)
- **복귀완료** (`returned`): 수동 수령 필요
  - 수령 시: 무기 CR/s × 임무 시간 크레딧 지급, 파일럿 인벤토리 반환, 슬롯 offline 복귀
- `DispatchManager._process()` — 매 프레임 타이머 체크, 상태 자동 전환
- `get_machine_preview()` — 머신 스펙 미리보기 (임무시간·복귀시간·예상수익·CR/s)

## 브릿지 — 파견 현황 표시 구현 완료

- 브릿지 하단에 파견 현황 상시 표시
- 임무중 슬롯: 슬롯 번호·목적지 + 임무 완료까지 카운트다운 (MM:SS)
- 귀환중 슬롯: 시안 + 복귀까지 카운트다운 (MM:SS)
- 파견 없음: 흐린 안내 문구

## 저장/불러오기 및 루프 내구성 구현 완료

- **SaveManager autoload** (`scripts/autoload/save_manager.gd`)
  - 저장: `user://save.json` (JSON, 탭 들여쓰기)
  - 자동 저장: `credits_changed`, `auto_slot_changed` 시그널 연결 — 주요 이벤트마다 즉시 저장
  - 앱 종료(`NOTIFICATION_WM_CLOSE_REQUEST`) 시 최종 저장 후 종료
- **자동로드 순서**: GameState → PanelManager → SaveManager (패널 초기화 전에 세이브 데이터 적용)
- **오프라인 진행 계산**: 로드 시 `now - save_time` 경과 시간으로 임무/귀환 상태 패스트포워드
  - `on_mission` → 임무 완료 시점 지났으면 credits_earned 계산 후 `returning` 전환
  - `returning` → 귀환 완료 시점 지났으면 `returned` 전환

## 자동 재파견 구현 완료 (UI 비활성, 백엔드 유지)

- `AutoSlot`에 `auto_redispatch / auto_pilot_id / auto_planet` 필드 추가
- 귀환 완료(`_complete_return`) 시 auto=ON이면: 크레딧 수령 + 파일럿 반환 + 즉시 동일 조건 재파견
- 저장/불러오기에 auto 설정 포함 — UI 토글은 현재 비활성 상태

## 현재 상태 요약

- 직접 파견 + 자동 파견 기본 플레이 루프 모두 완성.
- 파츠 구매 → 조립 → 파견 → 귀환 → 수령 → 재투자 전체 사이클 동작.
- 파츠 개별 아이템 시스템 리팩터링 완료 (v2→v3 세이브 마이그레이션 포함).
- 세이브 v4 전환 완료 — `hangar_groups` 직렬화 추가, v3→v4 자동 마이그레이션 내장.
- 공작실 인벤토리 탭 구현 완료 (동적 스크롤 그리드, 상세 패널).
- PC 터미널 메뉴형 상점 UI 확정 — 파츠 / 함선 강화(클릭 강화 + 자동 공격) / 파일럿 + 향후 카테고리 슬롯.
- 함선 강화: 자동 공격 업그레이드 구현 완료 (800 CR, click_damage/2, 1.5초 인터벌).
- 전 패널 UI 1920×300 해상도 최적화 완료 (배경 이미지 + 반투명 UI).
- 브릿지 유틸 사이드 패널 — 설정·사운드 기능 구현 완료.
- 저장/불러오기, 오프라인 진행, 자동 재파견 구현 완료.
- 파일럿 캐릭터 시스템 구현 완료 — 개별 인스턴스, 고정 풀 3인, 커스텀 생성, 로밍 애니메이션.
- 파일럿-베이 사전 배정 시스템 구현 완료 (`assigned_pilot_id`, `assign_pilot_to_slot`, `disassemble_machine`).
- 파견 관제 패널 전면 재설계 — anchor Tween 애니메이션, 전체 슬롯 세로 스크롤, 행성 compact 가로 스크롤.
- 패널 내비게이션 히스토리 스택 + 동적 뒤로가기 라벨 구현 완료 (클리커 히스토리 제외 처리).
- **ShipCanvas 파노라마 함선 캔버스 구현 완료** — 격납고/브릿지/관제실 3구역, 탭 네비게이션, 우클릭 드래그.
- **HangarZone 격납고 구역 구현 완료** — 4격납고×4베이 구조, 오버레이 팝업 방식 상세, 격납고 잠금/해금, 구역 격벽.
- **StarMapPopup 항성지도 팝업 구현 완료** — 행성 아이콘 그리드, 행성 슬롯 4행 그리드, 베이 팝업, 즉시 파견.
- 행성 몬스터 수 ×10 (섹터A 20/50, 섹터B 30/80, 섹터C 40/120).
- 초기 자금 0 CR, Windows exe 빌드 완료.
- 행성별 그래픽, BGM/SFX, 자동 재파견 다중 사이클 오프라인 계산은 미구현.
